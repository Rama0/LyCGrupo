init { /* Esto es un bloque de declaracion de variables */
        a,a2,a3 : Float
        variable : Int
        p1,p2,p3 : String
        variableK : Float
}
while(a>a2 & a3 <variableK)
{
    a=a+1;
}

if(a>a2 || a3 <variableK)
{
    if(variable>3)
    {
        read(p1);
        write(p1);
    }
    else
    {
        variable=203;
    }
}

variable=iguales( 10.5, [2*5+0.5,3.2,a3] );

do a
case variable==101
a=a+1;
case variable>202
a=a+2;
default
a=a+3;
enddo